# Background Remover

Приложение для удаления фона с фотографий с минималистичным черно-белым дизайном, с поддержкой множественных моделей API и интеграцией с Яндекс Диском.

## Возможности

- 🎨 Минималистичный черно-белый современный дизайн
- 🖼️ Удаление фона через различные API (Remove.bg, Clipdrop, Replicate, FAL)
- ☁️ Интеграция с Яндекс Диском для пакетной обработки
- 📁 Автоматическая обработка папок с фотографиями товаров
- 🎯 Размещение обработанных фото на шаблон
- 💾 Сохранение результатов в папку "Обработанные"
- ⚡ FastAPI backend с async поддержкой
- 🐳 Docker поддержка для легкого деплоя

## Локальная установка

### Вариант 1: С виртуальным окружением (.venv) - рекомендуется

1. Установите Python 3.8+ если еще не установлен

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
```

3. Активируйте виртуальное окружение:
   - На macOS/Linux:
   ```bash
   source .venv/bin/activate
   ```
   - На Windows:
   ```bash
   .venv\Scripts\activate
   ```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Создайте файл `.env` на основе `.env.example` и заполните API ключи:
```bash
cp .env.example .env
```

6. Настройте Яндекс Диск OAuth:
   - Создайте приложение на https://oauth.yandex.ru/
   - Укажите redirect URI: `http://localhost:8000/auth/yandex/callback`
   - Добавьте Client ID и Client Secret в `.env`

7. Запустите сервер:
```bash
python main.py
```
или с автоперезагрузкой:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

8. Откройте браузер: `http://localhost:8000`

9. Для деактивации виртуального окружения:
```bash
deactivate
```

### Вариант 2: Прямой запуск (без виртуального окружения)

1. Установите Python 3.8+ если еще не установлен

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example` и заполните API ключи:
```bash
cp .env.example .env
```

4. Настройте Яндекс Диск OAuth:
   - Создайте приложение на https://oauth.yandex.ru/
   - Укажите redirect URI: `http://localhost:8000/auth/yandex/callback`
   - Добавьте Client ID и Client Secret в `.env`

5. Запустите сервер:
```bash
python main.py
```
или с автоперезагрузкой:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

6. Откройте браузер: `http://localhost:8000`

### Вариант 2: Docker Compose (рекомендуется)

1. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

2. Заполните все необходимые переменные в `.env`

3. Запустите через Docker Compose:
```bash
docker-compose up --build
```

4. Откройте браузер: `http://localhost:8000`

Для остановки:
```bash
docker-compose down
```

### Вариант 3: Docker напрямую

1. Соберите образ:
```bash
docker build -t background-remover .
```

2. Запустите контейнер:
```bash
docker run -p 8000:8000 --env-file .env background-remover
```

## Деплой на Railway

### Подготовка

1. Убедитесь, что у вас есть аккаунт на [Railway](https://railway.app/)

2. Установите Railway CLI (опционально):
```bash
npm i -g @railway/cli
```

### Деплой через GitHub

1. Создайте репозиторий на GitHub и запушьте код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-github-repo-url>
git push -u origin main
```

2. Зайдите на [Railway Dashboard](https://railway.app/dashboard)

3. Нажмите "New Project" → "Deploy from GitHub repo"

4. Выберите ваш репозиторий

5. Railway автоматически определит Dockerfile и начнет сборку

6. Добавьте переменные окружения в настройках проекта (используйте те же значения, что и в локальном `.env` файле):
   - `REMOVEBG_API_KEY` - ваш API ключ Remove.bg
   - `CLIPDROP_API_KEY` - ваш API ключ Clipdrop
   - `REPLICATE_API_KEY` - ваш API ключ Replicate
   - `FAL_KEY` - ваш API ключ для FAL AI
   - `YANDEX_DISK_CLIENT_ID` - Client ID из `.env` (461caa34915949708dbb3b8c74c28ff9)
   - `YANDEX_DISK_CLIENT_SECRET` - Client Secret из `.env` (9ec7eb4802004459b64a94a199848641)
   - `YANDEX_DISK_REDIRECT_URI` - Redirect URI из `.env` (https://oauth.yandex.ru/verification_code)

7. Railway автоматически установит `PORT` переменную

**Примечание:** Для локальной разработки используйте файл `.env` в корне проекта. Для Railway добавьте те же переменные через веб-интерфейс или Railway CLI (`railway variables set KEY=value`).

### Деплой через Railway CLI

1. Войдите в Railway:
```bash
railway login
```

2. Инициализируйте проект:
```bash
railway init
```

3. Загрузите переменные окружения:
```bash
railway variables
# Или создайте railway.toml с переменными
```

4. Задеплойте:
```bash
railway up
```

### Важные замечания для Railway

- Railway автоматически устанавливает переменную `PORT` - убедитесь, что ваш код использует её
- Для Яндекс Диска OAuth укажите правильный redirect URI: `https://your-app.railway.app/auth/yandex/callback`
- Шаблоны сохраняются в файловую систему контейнера (эфемерно), для постоянного хранения используйте Railway Volumes или внешнее хранилище

## Использование

### Обработка одного изображения

1. Выберите модель API из списка
2. Введите API ключ и нажмите "Сохранить"
3. Загрузите изображение (drag & drop или клик)
4. Нажмите "Обработать"
5. Скачайте результат

### Пакетная обработка через Яндекс Диск

1. Нажмите "Авторизоваться" для Яндекс Диска
2. Выберите папку с фотографиями товаров
3. Нажмите "Обработать папку"
4. Обработанные фото будут сохранены в папку "Обработанные"

## Структура проекта

```
background_remover/
├── index.html          # Главный HTML
├── styles.css          # Минималистичный CSS стили
├── app.js              # Логика приложения
├── main.py             # FastAPI сервер
├── requirements.txt    # Python зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── railway.json        # Railway конфигурация
└── templates/          # Шаблоны для размещения фото
    └── default.png
```

## API Модели

### Remove.bg
- Получите API ключ на https://www.remove.bg/api

### Clipdrop
- Получите API ключ на https://clipdrop.co/api

### Replicate
- Получите API ключ на https://replicate.com/account/api-tokens

### FAL
- Используется через fal-client (fal.ai)
- Получите API ключ на https://fal.ai/
- Установите `FAL_KEY` в `.env`

## Технологии

- **Frontend**: Vanilla JS, HTML, CSS
- **Backend**: FastAPI (Python)
- **Image Processing**: Pillow
- **FAL**: fal-client (async)
- **Containerization**: Docker
- **Deployment**: Railway

## Переменные окружения

См. `.env.example` для полного списка переменных окружения.

## Лицензия

MIT
